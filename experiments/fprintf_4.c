////////////////////////////////////////////////////////////////////////////////
// Comments

// Explores ways of getting data into stdin.

////////////////////////////////////////////////////////////////////////////////
// Libraries

#include <stdio.h>

////////////////////////////////////////////////////////////////////////////////
int main (void) {
	int c, e;
	char s[] = "12345";

	// Doesn't work
	//fprintf (stdin, "%s", s);
	
	for (e = 0; s[e] != '\0'; ++e)
		ungetc (s[e], stdin);

	while ((c = getchar ()) != EOF)
		printf ("%c\n", c);

	return 0;
}
