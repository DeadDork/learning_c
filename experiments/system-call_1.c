////////////////////////////////////////////////////////////////////////////////
// Comments

// Tests making a system call.

////////////////////////////////////////////////////////////////////////////////
// Libraries

#include <stdio.h>

////////////////////////////////////////////////////////////////////////////////
int main( void )
{
	int c; // Character
	FILE * shout; // SHell OUTput

	shout = popen( "stty " "-g", "r" );
	
	while( ( c = fgetc( shout ) ) != EOF )
	{
		putchar( c );
	}

	pclose( shout );

	return 0;
}
