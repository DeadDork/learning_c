////////////////////////////////////////////////////////////////////////////////
// Comments

// Tests the length of an empty char pointer.

// Nope, this doesn't explain why regex-substring_1.c isn't working, either.

////////////////////////////////////////////////////////////////////////////////
// Libraries

#include <stdio.h>
#include <stdlib.h>
#include "sstring.h"

////////////////////////////////////////////////////////////////////////////////
// Function Prototypes

void subprint( char *c );

////////////////////////////////////////////////////////////////////////////////
// Functions

void subprint( char *c )
{
	char *tmp, *ttmp;

	tmp = c;
	ttmp = tmp;
	while( *ttmp != '\0')
	{
		printf( "length of empty pointer = [%d]\n", slength( ttmp ) );
		printf( "pointer = [%s]\n", ttmp );
		exit( 0 );
	}
}

////////////////////////////////////////////////////////////////////////////////
int main()
{
	char *c;

	subprint( c );

	return 0;
}
